<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.farnadsoft.onlineshop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.farnadsoft.onlineshop.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Store"
        android:usesCleartextTraffic="true">
        <activity android:name="com.farnadsoft.onlineshop.feature.auth.AuthActivity" android:theme="@style/Theme.Store.AuthTheme"></activity>
        <activity android:name="com.farnadsoft.onlineshop.feature.list.ProductListActivity"></activity>
        <activity android:name="com.farnadsoft.onlineshop.feature.product.comment.CommentListActivity" />
        <activity android:name="com.farnadsoft.onlineshop.feature.product.ProductDetailActivity" />
        <activity android:name="com.farnadsoft.onlineshop.feature.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>